---
import { Picture } from "@astrojs/image/components";
import heroImage from "assets/UIMock2.png";
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      src={heroImage}
      alt="ReferBlock UI Mockup"
      widths={[200, 400, 600]}
      aspectRatio="4:3"
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
    />
  </div>
  <div>
    <h1
      style="font-family: 'Merriweather Sans', sans-serif; background: linear-gradient(to right, #5E8BFF, #AD60FF); -webkit-background-clip: text; color: transparent; font-weight: bold;"
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      Infrastructure for Influencer and Affiliate Marketing
    </h1>
    <p
      style="font-family: 'Lato', sans-serif; color:#1A2644; font-weight: bold; background-color: transparent;"
      class="text-lg mt-4 text-slate-600 max-w-xl">
      Referee makes influencer marketing easy. Transparent transactions and
      automation create trust so business alignment can happen instantly.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="/contact"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        Sign Up
      </Link>
      <!--
      <Link
        size="lg"
        style="outline"
        rel="noopener"
        href="https://github.com/surjithctly/astroship"
        class="flex gap-1 items-center justify-center"
        target="_blank">
        <Icon class="text-black w-4 h-4" name="bx:bxl-github" />
        GitHub Repo
      </Link>
       -->
    </div>
  </div>
</main>
